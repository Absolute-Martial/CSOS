# Environment variables template for Dokploy deployment
# Copy this to your Dokploy service environment settings

# ===========================================
# BACKEND SERVICE (.env)
# ===========================================

# Database - Replace with your Dokploy PostgreSQL internal URL
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@postgres-service.dokploy.internal:5432/engineering_os

# Copilot API - Local only (requires GitHub OAuth)
# Run locally: npx copilot-api@latest start
COPILOT_API_URL=http://localhost:4141

# File uploads directory
UPLOAD_DIR=/app/uploads

# C Engine path (inside Docker container)
ENGINE_PATH=/app/engine/scheduler

# ===========================================
# FRONTEND SERVICE (.env)
# ===========================================

# Backend API URL - Replace with your Dokploy backend URL
NEXT_PUBLIC_API_URL=https://your-backend.your-domain.com

# ===========================================
# DATABASE SERVICE
# ===========================================

POSTGRES_USER=postgres
POSTGRES_PASSWORD=YOUR_SECURE_PASSWORD
POSTGRES_DB=engineering_os
